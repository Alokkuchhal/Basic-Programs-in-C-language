#include<stdio.h>
#include<conio.h>
int main()
{
    float a,b,c;
    printf("Enter the base and height of the triangle .");
    scanf("%f %f",&a,&b);
    c=(a*b)/2;
    printf("The area of the triangle is %f.",c);
    getch();

}
